# Autograding configuration for GitHub Classroom
# You can fully customize this file

# 🔹 Setup: Install dependencies before running tests
setup:
  - pip install pytest

# 🔹 Test cases
tests:
  - name:  Run all Pytest tests
    run: pytest -q test_m2_1.py
    timeout: 20
    points: 30

  - name:  Check specific function
    run: pytest -q test_m2_1.py::test_specific_case
    timeout: 10
    points: 40

  - name:  Edge case test
    run: pytest -q test_m2_1.py::test_edge_case
    timeout: 10
    points: 30

# 🔹 Optional: Customize environment variables
env:
  PYTHONPATH: "."

# 🔹 Optional: Extra grading rules
# (e.g., check formatting, linting, etc.)
# tests:
#   - name: Code Style (flake8)
#     run: flake8 m2_1.py
#     timeout: 15
#     points: 5